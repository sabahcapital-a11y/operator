// Auto-generated by scripts/build-blog.ts — do not edit by hand
// Generated at: 2026-07-22T23:46:44.787Z

export interface BlogPost {
  slug: string;
  title: string;
  date: string;
  excerpt: string;
  html: string;
  description: string;
}

const blogPosts: BlogPost[] = [
  {
    "slug": "wordpress-plugin-update-broke-site",
    "title": "A WordPress Plugin Update Broke Your Client's Site. Here's How to Find It (And Prevent It Next Time)",
    "date": "2026-07-22",
    "excerpt": "Monday, 8:47 AM. Your phone rings. It's the client whose site you launched six months ago — the one that's been running flawlessly ever since. You pick up, expecting a \"quick question\" about a new ...",
    "html": "<p>Monday, 8:47 AM. Your phone rings. It's the client whose site you launched six months ago — the one that's been running flawlessly ever since. You pick up, expecting a \"quick question\" about a new landing page.</p>\n<p>\"Hey,\" they say, voice tight. \"Our site is... wrong. The checkout page just shows a white screen. And three customers emailed saying they couldn't submit the contact form this weekend.\"</p>\n<p>You pull up the site. The homepage loads fine. But the pricing page is a mess — broken CSS, a giant PHP error where the CTA button should be, and the footer has migrated halfway up the screen. You log into WordPress and check the updates log.</p>\n<p>Six plugins auto-updated at 3:14 AM on Saturday. One of them — a seemingly minor \"security patch\" to a form builder — took down the checkout, the contact form, and the pricing page in one shot. It's been broken for 60 hours. And the only person who noticed was a frustrated customer.</p>\n<p>This isn't a horror story. It's Tuesday for any agency managing WordPress sites at scale. Here's why it happens, how to find the culprit fast, and how to stop it from happening again.</p>\n<h2>Why Auto-Updates Are a Double-Edged Sword</h2>\n<p>WordPress 5.5 introduced auto-updates for plugins and themes in 2020, and since then, auto-updates have become the default on most managed WordPress hosts. The logic is sound: outdated plugins are the #1 attack vector for WordPress sites. A security patch that sits unapplied for three weeks is a ticking bomb.</p>\n<p>But the implementation has a blind spot the size of a freight truck: <strong>WordPress has no way to know if an update broke anything.</strong> It can update the plugin. It can check if the site still loads (barely). It cannot check if the contact form still delivers email, if the checkout calculates tax correctly, or if the booking widget still renders on mobile.</p>\n<p>So WordPress updates the plugin, the homepage still returns a 200, and WordPress declares victory. Meanwhile, three mission-critical features are silently dead.</p>\n<p>Here's what's actually happening under the hood when an auto-update breaks your client's site.</p>\n<h3>The Most Common Failure Modes</h3>\n<p><strong>Plugin-on-plugin conflict (the silent killer).</strong> Plugin A updates and changes how it hooks into <code>wp_enqueue_scripts</code>. Plugin B was relying on the old hook order. Neither throws an error — Plugin B just stops loading its CSS, or its JavaScript initializes before the library it depends on is ready. The site looks fine until a visitor tries to use the feature that requires both plugins to cooperate.</p>\n<p><strong>PHP version mismatch.</strong> The plugin's changelog says \"Compatibility: WordPress 6.4+, PHP 7.4+.\" What it doesn't say is that the new version uses a PHP 8.0 feature (named arguments, union types, <code>str_contains</code>) that silently fatals on PHP 7.4. The client's host is still running PHP 7.4 because \"if it ain't broke\" — and now it's broke. The fatal error only triggers when a specific function path is hit, which is why the homepage loads fine but the checkout dies.</p>\n<p><strong>Database schema changes that don't roll back.</strong> Some plugins add or modify database tables during an update. If the update adds a column and the <code>ALTER TABLE</code> fails (locked table, permission issue, different MySQL version), the plugin may continue running in a partially-broken state — it expects a column that doesn't exist, so queries fail silently. No error on screen. Just missing data.</p>\n<p><strong>jQuery version collision.</strong> WordPress bundles jQuery, but many themes and plugins load their own versions — or depend on specific jQuery features that were deprecated between versions. A plugin update that switches from jQuery 3.x APIs to native JavaScript can break another plugin that was piggybacking on the old jQuery instance. The console fills with <code>$ is not a function</code> or <code>Uncaught TypeError: $(...).live is not a function</code>, but nobody's watching the console.</p>\n<p><strong>Composer dependency drift.</strong> Many modern plugins ship with a <code>vendor/</code> directory containing Composer-managed PHP libraries. When two plugins bundle different versions of the same library (say, Plugin A has Monolog 2.x and Plugin B has Monolog 3.x), PHP loads whichever one executes first and hopes for the best. The result: one plugin works, the other throws a fatal error about a missing class method. This is called a \"dependency conflict\" and it's essentially undetectable until someone exercises the broken code path.</p>\n<h2>How to Find Which Plugin Update Broke What</h2>\n<p>The standard advice — \"disable all your plugins and re-enable them one by one\" — works. But it's the nuclear option, and it creates its own problems: disabling a plugin can wipe its settings, break widget configurations, or log out all users. Plus, if the site is actively serving traffic, you're now testing in production while the problem is still live.</p>\n<p>Here's a faster, more surgical approach.</p>\n<h3>Step 1: Check the Update Timestamps (60 Seconds)</h3>\n<p>Go to <strong>Plugins → Installed Plugins</strong>. In the list view, most WordPress installs show a \"Last Updated\" column. If yours doesn't, go to <strong>Dashboard → Updates</strong> and click \"View version details\" on any recently updated plugin — it'll show the update date.</p>\n<p>Cross-reference with the client's report of when things broke. \"The checkout stopped working Saturday\" plus \"WooCommerce Payments updated Saturday at 3:14 AM\" is not a coincidence. You now have a suspect list, usually 1–3 plugins.</p>\n<h3>Step 2: Enable WordPress Debug Log (2 Minutes)</h3>\n<p>Add this to <code>wp-config.php</code>:</p>\n<pre><code>define('WP_DEBUG', true);\ndefine('WP_DEBUG_LOG', true);\ndefine('WP_DEBUG_DISPLAY', false);</code></pre>\n<p>This writes errors to <code>/wp-content/debug.log</code> without displaying them on the front end. Now reproduce the broken action — submit the form, hit the checkout, load the page that's white-screening — and then check the log:</p>\n<pre><code>tail -50 wp-content/debug.log</code></pre>\n<p>You're looking for <code>PHP Fatal error</code>, <code>Uncaught Error</code>, or <code>Call to undefined function</code>. The stack trace will tell you exactly which plugin file is throwing the error. This alone solves about 60% of post-update breaks in under five minutes.</p>\n<h3>Step 3: Check for JavaScript Breakage (3 Minutes)</h3>\n<p>Not every break throws a PHP error. If the page loads but the form button does nothing, the problem is client-side.</p>\n<p>Open Chrome DevTools <strong>(F12 → Console)</strong> and reproduce the action. You're looking for:</p>\n<ul><li><strong>Red errors</strong> — <code>Uncaught TypeError</code>, <code>ReferenceError</code>, <code>Cannot read properties of null</code>. Click the file link on the right of the error and it'll open the exact line of the minified JS that's crashing. The file path will contain the plugin slug.</li>\n<li><strong>Yellow warnings with <code>deprecated</code></strong> — these were warnings before the update and errors after. A plugin that updated its dependency may have removed a function the old plugin was calling.</li>\n<li><strong>Failed network requests</strong> — switch to the Network tab, filter by \"JS,\" and reload. Any red (4xx/5xx) <code>.js</code> file is a plugin script that isn't loading. The URL path tells you which plugin.</li>\n</ul>\n<h3>Step 4: The Selective Rollback Test (5 Minutes)</h3>\n<p>Instead of disabling every plugin, install the <a href=\"https://wordpress.org/plugins/wp-rollback/\" class=\"text-blue-600 hover:underline\">WP Rollback</a> plugin. It adds a \"Rollback\" link to every plugin in your installed list. Pick your prime suspect from Step 1 and roll it back one version. Test the broken feature.</p>\n<p>If the feature works after rolling back Plugin A, you've found the culprit. Re-update Plugin A to confirm the break reoccurs. Now you know exactly which update broke the site, and you can either keep it rolled back until the developer ships a fix, or dig into the changelog to find the specific change.</p>\n<h3>Step 5: Last Resort — Health Check & Troubleshooting Mode</h3>\n<p>If none of the above identified the problem, WordPress has a built-in tool that's better than the nuclear option. Install the <a href=\"https://wordpress.org/plugins/health-check/\" class=\"text-blue-600 hover:underline\">Health Check & Troubleshooting</a> plugin. Its \"Troubleshooting Mode\" selectively disables plugins <em>only for your logged-in session</em> — the rest of the world sees the site in its normal (broken) state. You can enable plugins one at a time in troubleshooting mode and test without affecting visitors.</p>\n<p>This is the \"disable everything\" approach, but without the production impact. Use it when the debug log is empty and the console is clean but the break is still happening — which usually means the problem is a data conflict, not a code crash.</p>\n<h2>The Fix, Depending on What You Found</h2>\n<h3>You Found the Plugin That Broke</h3>\n<p>You have three options, in order of preference:</p>\n<li><strong>Roll back to the previous version</strong> (WP Rollback, or download the old version from the plugin's Advanced View on WordPress.org). Wait for the developer to ship a fix. Set a calendar reminder to check for the next update in two weeks.</li>\n<li><strong>Check the plugin's support forum.</strong> If the update broke your client's site, it almost certainly broke other people's sites too. The plugin's WordPress.org support forum or GitHub issues page will have threads about it, often with temporary patches or workarounds posted by other developers.</li>\n<li><strong>Patch the specific change yourself.</strong> If the break is a single function call or a deprecated API, and you're comfortable writing PHP, you can patch the plugin file directly. This is a temporary fix — your patch will be overwritten by the next update — so file an issue with the developer and only do this as a stopgap.</li>\n<h3>You Found a PHP Version Problem</h3>\n<p>Check the server's PHP version: <strong>Tools → Site Health → Info → Server</strong>. If it's below the plugin's minimum requirement, you have two choices: upgrade PHP on the host (ideal, but may break <em>other</em> things), or roll back the plugin and pin it until the host can be upgraded in a controlled way.</p>\n<p>Most managed WordPress hosts let you switch PHP versions from the control panel. Test on a staging site first — a PHP version bump can surface compatibility issues that have been dormant for years.</p>\n<h3>You Found a jQuery or JavaScript Conflict</h3>\n<p>If the break is in a <code>.js</code> file that comes from your theme or another plugin, not the one that updated — meaning Plugin A updated and broke Plugin B's JavaScript — you have a conflict. The fix is usually to enqueue scripts in the correct order with the correct dependencies. In your theme's <code>functions.php</code> or a custom plugin:</p>\n<pre><code>// If Plugin B's script depends on jQuery, make it explicit\nwp_enqueue_script('plugin-b-script', '...', array('jquery'), '1.0', true);</code></pre>\n<p>The <code>array('jquery')</code> part tells WordPress to load jQuery before this script. If Plugin A removed jQuery as a dependency and Plugin B was implicitly relying on it, this fix makes the dependency explicit.</p>\n<h2>Prevention: Stop Auto-Updates From Ambushing You</h2>\n<p>You can't eliminate the risk of plugin updates — but you can make sure you're the one who discovers the break, not the client.</p>\n<h3>1. Selective Auto-Update Disabling</h3>\n<p>Auto-updates for security patches are good. Auto-updates for everything are reckless. Disable auto-updates selectively by adding this to <code>wp-config.php</code>:</p>\n<pre><code>define('WP_AUTO_UPDATE_CORE', false); // Never auto-update WordPress core</code></pre>\n<p>Then, in the plugin list, disable auto-updates for plugins that are mission-critical (forms, checkout, booking, page builders) and keep them on for everything else. Or use a filter in a mu-plugin:</p>\n<pre><code>add_filter('auto_update_plugin', function($update, $item) {\n    $no_auto = ['gravityforms', 'woocommerce', 'elementor-pro'];\n    return in_array($item-&gt;slug, $no_auto) ? false : $update;\n}, 10, 2);</code></pre>\n<p>Now security-only plugins update automatically, and revenue-critical plugins wait for you.</p>\n<h3>2. Staging Sites (That You Actually Use)</h3>\n<p>Every managed WordPress host offers staging. Almost nobody uses it consistently. Here's a workflow that takes 10 minutes per update batch:</p>\n<ul><li>Once a week, push production to staging (one click on most hosts)</li>\n<li>Run all pending plugin updates on staging</li>\n<li>Run through a checklist: submit the contact form, complete a test purchase, book an appointment, check the tracking pixel</li>\n<li>If everything passes, apply updates to production</li>\n</ul>\nThis catches 90% of update-related breaks before they hit real visitors. The problem is the checklist step — it's tedious enough that agencies skip it. We'll come back to this.\n<h3>3. Update Policies That Protect Revenue</h3>\n<p>Set a client-facing policy: major plugin updates (WooCommerce, Elementor, Gravity Forms) are applied on a schedule, not automatically. Your retainer agreement should state that updates are tested on staging first, and major version bumps (3.x → 4.x) get a dedicated testing window. This turns \"the update broke my site\" from an emergency into a managed process, and it sets expectations that updates aren't invisible — they require work.</p>\n<h3>4. Automated Post-Update Testing</h3>\n<p>The staging checklist is the right idea, but \"submit the form and check the email\" doesn't scale past five client sites. You need something that does it automatically, every time, without you remembering.</p>\n<p>That means a tool that, after every plugin update batch, simulates a real visitor: fills out the contact form, confirms the email arrived and the CRM received the lead; goes through the checkout flow, confirms the payment gateway didn't error; loads the booking widget, confirms it renders and accepts clicks. On mobile viewports. With the tracking pixels verified.</p>\n<p>If any of those checks fail, you get an alert. Not the client.</p>\n<h2>The Real Issue Isn't the Plugin Update</h2>\n<p>Plugin updates are going to happen. Security patches are going to auto-apply. Developers are going to ship breaking changes. The problem isn't that updates break things — it's that you find out from a client phone call, days later, after real revenue has been lost.</p>\n<p>Every minute between \"the plugin updated\" and \"someone noticed it broke\" is a minute your client's site is leaking money. The agencies that thrive after the first major update disaster are the ones that close that gap to zero — by making post-update verification automatic, not manual.</p>\n<p><strong>Want every form, widget, and tracking pixel re-verified automatically after every update?</strong> <a href=\"https://leadguard.dev\" class=\"text-blue-600 hover:underline\">Run LeadGuard's free scan →</a> — paste any client URL and we'll test their contact forms, booking widgets, checkout flows, and tracking pixels in under two minutes. No credit card, no scripts, no setup. If something breaks at 3 AM, you'll know before the client does.</p>",
    "description": "Monday, 8:47 AM. Your phone rings. It's the client whose site you launched six months ago — the one that's been running flawlessly ever since. You pick up, e..."
  },
  {
    "slug": "ga4-conversions-dropped-to-zero",
    "title": "GA4 Conversions Dropped to Zero? Here's What Actually Broke (And How to Fix It)",
    "date": "2026-07-22",
    "excerpt": "You open Google Analytics 4, click over to your client's conversion report, and your brain does a double-take.",
    "html": "<p>You open Google Analytics 4, click over to your client's conversion report, and your brain does a double-take.</p>\n<p>The line that was tracking 40–50 conversions per day — form submissions, purchases, booked calls — is flat. Zero. Not \"down a bit.\" Not \"weird dip on Tuesday.\" A perfect, unbroken horizontal line, like the graph itself gave up.</p>\n<p>Your first thought: <em>Maybe nobody converted today.</em> Unlikely, but you check the client's CRM just in case. Nope — five new leads came in. They just never showed up in GA4.</p>\n<p>Something broke. And like most things in Google's ecosystem, it broke silently.</p>\n<p>Here's what's actually going wrong and how to fix it before your next client reporting call.</p>\n<h2>What Takes Down GA4 Conversions Overnight</h2>\n<p>GA4 conversions don't just \"stop working.\" Something changed. The trick is figuring out what — because none of these failures throw an error message.</p>\n<h3>1. Someone Touched Google Tag Manager (And Didn't Tell You)</h3>\n<p>This is the #1 culprit. A team member opens GTM to add a new Meta pixel or tweak a trigger for a landing page. They publish the container. Unbeknownst to them, they've also:</p>\n<ul><li>Accidentally edited or deleted the GA4 configuration tag</li>\n<li>Changed the firing trigger for your conversion event (e.g., from \"all thank-you pages\" to \"only the homepage\" — yes, this happens)</li>\n<li>Published a container version they thought was a draft</li>\n<li>Removed a variable that the conversion tag depends on</li>\n</ul>\nGTM doesn't warn you when you break something downstream. It publishes whatever's in the container, and GA4 goes dark.\n<p><strong>How GTM changes kill conversions:</strong> Your GA4 event tag (like <code>generate_lead</code> or <code>purchase</code>) fires when a specific trigger condition is met — usually a page view on <code>/thank-you</code> or a click on the submit button. If someone changes that trigger, removes the tag, or breaks a variable the tag uses, the event stops firing entirely. GA4 never sees it.</p>\n<h3>2. The GA4 Tag Went Missing From Key Pages</h3>\n<p>This one's maddening because the tag fires fine on the homepage — you can see yourself in the realtime report. But it's missing from the actual conversion pages: the booking confirmation, the checkout success screen, the \"thanks for reaching out\" page.</p>\n<p>Common causes:</p>\n<ul><li>A developer removed GTM from the thank-you page template during a redesign and forgot to add it back</li>\n<li>The site switched page builders and the new builder doesn't inherit the header/footer where GTM is embedded</li>\n<li>A consent management platform (Cookiebot, OneTrust, CookieYes) is blocking GTM from loading on certain pages until the user accepts cookies — and your thank-you page happens after a redirect where consent state gets lost</li>\n<li>The GA4 measurement ID is present in GTM but your GTM container snippet itself is missing from the page</li>\n</ul>\n<h3>3. The Consent Banner Is Silently Blocking Everything</h3>\n<p>GA4 respects consent signals. If you're using Consent Mode v2 and your consent banner isn't configured correctly, GA4 will receive a <code>denied</code> signal and fire cookieless pings — which look like they're working in realtime reports but never register as conversions in your actual reports.</p>\n<p>This one's particularly sneaky because:</p>\n<ul><li>Realtime shows <em>something</em> happening</li>\n<li>DebugView shows events arriving</li>\n<li>But conversions show zero because the consent state defaults to denied and the user never interacted with the banner</li>\n</ul>\nThe most common scenario: you implemented Consent Mode, set the default to <code>denied</code>, and your thank-you page loads after a redirect where the consent state from the previous page isn't carried over. GA4 fires without consent, the event arrives, but it's discarded for conversion counting.\n<h3>4. The Form Redirect Changed (Or Disappeared)</h3>\n<p>Your conversion tracking depends on the form redirecting to a <code>/thank-you</code> page after submission. Simple. Reliable. Until:</p>\n<ul><li>Someone updates the form plugin and the redirect URL gets wiped</li>\n<li>The form now uses an AJAX submission that never triggers a page view — the visitor stays on the same URL</li>\n<li>The client asked for an in-line \"thanks\" message instead of a redirect, so the thank-you page trigger never fires</li>\n<li>The redirect URL has a typo or trailing slash mismatch (<code>/thank-you</code> vs <code>/thank-you/</code>)</li>\n</ul>\nIf the trigger is \"Page View — URL contains /thank-you\" and the visitor never hits that URL, your conversion event never fires. Zero. Flatline.\n<h3>5. The GA4 Event Name Changed (Or Duplicated)</h3>\n<p>GA4 events are case-sensitive and name-exact. If your conversion is set to count <code>generate_lead</code> and your tag fires <code>generate_lead </code> (trailing space), <code>Generate_Lead</code> (capitalized), or <code>form_submission</code> (renamed during a cleanup), GA4 sees these as completely different events.</p>\n<p>Also common: someone duplicates a tag in GTM, gives it a slightly different event name, and now you have two tags firing similar-but-not-identical events. Your conversion report is counting <code>generate_lead</code> but the tag is sending <code>lead_generated</code>.</p>\n<h3>6. Cross-Domain Tracking Broke During a Platform Migration</h3>\n<p>If the conversion happens on a different domain than the main site (common with booking platforms like Calendly, Square Appointments, or third-party checkout pages), cross-domain tracking has to be configured correctly in GA4. If it isn't:</p>\n<ul><li>The user's session breaks between domains</li>\n<li>The conversion event fires on the booking domain</li>\n<li>GA4 can't connect it back to the original session or traffic source</li>\n<li>The conversion either disappears or gets attributed to \"Direct / None\"</li>\n</ul>\nThis breaks silently because the event <em>technically</em> fires — it just disappears into attribution purgatory.\n<h2>Diagnose It: Find the Break in 15 Minutes</h2>\n<p>Run these checks in order. You'll find the problem before step four.</p>\n<h3>Step 1: GA4 Realtime Report</h3>\n<p>Open GA4 → Reports → Realtime. Submit a test conversion yourself (fill out the form, complete the booking, whatever the conversion action is). Watch the realtime feed.</p>\n<ul><li><strong>If your test event appears in realtime</strong> but conversions are still zero in standard reports: the issue is likely consent mode (events arrive but are filtered) or the event isn't marked as a conversion in GA4 admin. Skip to Step 4.</li>\n<li><strong>If your test event does NOT appear in realtime:</strong> the tag isn't firing at all. Go to Step 2.</li>\n</ul>\n<h3>Step 2: GTM Preview Mode</h3>\n<p>Open Google Tag Manager → Preview. Enter the URL of your conversion page (or the page where the conversion starts). In the preview panel:</p>\n<li>Check that the GA4 configuration tag fires on <em>every page</em> — especially the thank-you/confirmation page</li>\n<li>Check that your conversion event tag fires when you complete the conversion action</li>\n<li>Look at the \"Tags Not Fired\" section — is your conversion tag listed there? If so, click it to see which trigger condition failed</li>\n<p>This tells you <em>exactly</em> what's blocking the tag. Usually it's a trigger mismatch: the URL doesn't match what the trigger expects, or a blocking trigger is preventing the tag.</p>\n<h3>Step 3: Chrome DevTools Network Tab</h3>\n<p>Open DevTools → Network tab. Filter by <code>collect</code> or <code>google-analytics</code>. Complete your test conversion.</p>\n<p>You should see a POST request to <code>https://analytics.google.com/g/collect</code> with parameters including <code>en=</code> (event name) and <code>ep.*</code> (event parameters). If you don't see this request at all, GTM isn't on the page or the GA4 tag isn't firing for that page.</p>\n<p>If you see the request but conversions are still zero, look at the <code>gcs</code> parameter in the payload — this is the consent state. <code>G100</code> means consent granted. <code>G110</code> means no consent, and that event won't count as a conversion.</p>\n<h3>Step 4: GA4 Admin — Check Conversion Settings</h3>\n<p>Go to Admin → Events → Conversions. Make sure the event you're tracking is actually marked as a conversion. If someone renamed the event or created a new one, the conversion flag might be on the old event name.</p>\n<p>Also check: Admin → Data Display → Key Events. In GA4's recent terminology shuffle, \"conversions\" are now \"key events.\" If your reports are using the old \"conversions\" metric and you haven't migrated, the numbers may show zero even though key events are tracking.</p>\n<h3>Step 5: Tag Assistant (Google's Official Debugger)</h3>\n<p>Install the <a href=\"https://tagassistant.google.com/\" class=\"text-blue-600 hover:underline\">Tag Assistant Companion extension</a>. It's more reliable than GTM Preview for catching consent-mode issues and cross-domain tracking problems. Enable it, complete your conversion, and check the report at https://tagassistant.google.com/.</p>\n<p>Tag Assistant will flag: missing consent signals, duplicate tags, cross-domain measurement failures, and configuration errors that GTM Preview doesn't surface.</p>\n<h2>The Fix for Each Cause</h2>\n<p><strong>GTM changes:</strong> Open the container's version history (Versions → click the version that was live when conversions worked). Compare with the current version. Find the tag or trigger that changed. Either revert or fix it. Test in Preview mode <em>before</em> publishing.</p>\n<p><strong>Missing GA4 tag on pages:</strong> Re-add the GTM container snippet to the page template. If the site uses a CMS, the header/footer should be global — check the theme settings. If the thank-you page uses a different template, that's your culprit. Add GTM there.</p>\n<p><strong>Consent banner blocking:</strong> Check your consent management platform's settings. Look for \"default consent state\" — if it's <code>denied</code> and your banner doesn't appear on the post-conversion page, you need to either: (a) configure the CMP to carry consent across redirects, or (b) set up Consent Mode in GTM with an update command that fires on the thank-you page after the user has consented.</p>\n<p><strong>Form redirect broken:</strong> Go to the form plugin settings. Check the confirmation/redirect URL. Fix it. Test the form end-to-end. If the form now uses AJAX (no redirect), you'll need to switch your GTM trigger from \"Page View\" to a Custom Event based on the form's JavaScript callback — usually a <code>form_submit_success</code> or similar event that the plugin fires after submission.</p>\n<p><strong>Event name mismatch:</strong> Pick one event name. Standardize. In GTM, rename the tag to match. In GA4, make sure that exact event name is marked as a conversion. Use lowercase, underscores, and no trailing spaces. Document it somewhere your team can find it.</p>\n<p><strong>Cross-domain tracking:</strong> In GA4 Admin → Data Streams → Configure Tag Settings → Configure your domains. Add every domain involved in the user journey. In GTM, add the GA4 configuration tag's \"domains\" field with a comma-separated list. Test with Tag Assistant to verify sessions carry across domains.</p>\n<h2>The Pattern Nobody Talks About</h2>\n<p>Look at the list above again. Every single cause shares one thing: nobody noticed until someone checked manually.</p>\n<p>GTM publishes silently. Consent banners block silently. Redirects break silently. There's no dashboard alert, no email notification, no warning from Google when your conversion tracking stops working. The only signal is the flatline — and you only see that when you happen to open GA4.</p>\n<p>For agencies managing 10, 20, 50 client sites, \"check GA4 every morning for every client\" isn't realistic. So the flatline goes unnoticed for days, sometimes weeks, until the client asks why their reported conversions don't match their actual sales — and now you're on a damage-control call instead of a proactive one.</p>\n<h2>Stop Finding Out From Your Clients</h2>\n<p>The fix for the flatline problem isn't better GTM hygiene (though that helps). It's knowing the flatline happened the day it started, not the day someone noticed.</p>\n<p>That's why we built LeadGuard. Every night, it simulates a real visitor going through your client's conversion path — filling out the form, completing the booking, hitting the thank-you page — and verifies that the GA4 conversion pixel fires. If it doesn't, you get an alert with a screenshot and a plain-English diagnosis before the client ever opens their dashboard.</p>\n<p>No scripts. No configuration. Paste a URL and LeadGuard's agents crawl the site, identify the conversion paths, and start testing.</p>\n<p><strong>Want your conversion tracking verified every night?</strong> <a href=\"https://leadguard.dev\" class=\"text-blue-600 hover:underline\">Run LeadGuard's free scan →</a> — paste any client URL and we'll test their forms, booking widgets, and tracking pixels in under two minutes. No credit card, no setup.</p>",
    "description": "You open Google Analytics 4, click over to your client's conversion report, and your brain does a double-take."
  },
  {
    "slug": "contact-form-not-sending-email",
    "title": "Contact Form Not Sending Email? Here's How to Fix It (Before You Lose Another Lead)",
    "date": "2026-07-22",
    "excerpt": "You're scrolling through old emails and something catches your eye. A lead replied to a follow-up saying \"I filled out your contact form two weeks ago and never heard back.\"",
    "html": "<p>You're scrolling through old emails and something catches your eye. A lead replied to a follow-up saying \"I filled out your contact form two weeks ago and never heard back.\"</p>\n<p>Your stomach drops.</p>\n<p>You check your form's email notifications. Nothing. You submit a test. Still nothing. That sinking realization hits: your contact form has been broken for <em>weeks</em>, and you have no idea how many leads evaporated during that time.</p>\n<p>This happens constantly — to agencies and their clients. The form looks fine on the front end. No error messages. No warning signs. It just... stops delivering.</p>\n<p>Here's what's actually breaking and how to fix each one.</p>\n<h2>Why Your Contact Form Stopped Sending (The Usual Suspects)</h2>\n<h3>1. Your WordPress Host Has PHP Mail Disabled (or Broken)</h3>\n<p>Most shared hosts and managed WordPress platforms turn off PHP's <code>mail()</code> function outright — it's a spam vector. If your form plugin relies on <code>wp_mail()</code>, which defaults to PHP mail, those emails go straight to <code>/dev/null</code>. No bounce, no error, no trace.</p>\n<p><strong>How to check:</strong> Install a plugin like \"Check Email\" or \"WP Mail SMTP\" and send a test. If the test never arrives, PHP mail is the culprit.</p>\n<h3>2. Your SMTP Plugin Died Quietly</h3>\n<p>You set up SMTP (SendGrid, Mailgun, Gmail SMTP, or your client's Microsoft 365 account) months ago. Then one of these happened:</p>\n<ul><li>The API key expired</li>\n<li>The sending limit was hit</li>\n<li>Microsoft 365 disabled basic authentication</li>\n<li>The SMTP plugin was deactivated during a \"cleanup\" of unused plugins</li>\n</ul>\n<strong>How to check:</strong> Open your SMTP plugin's settings page and use its built-in test email feature. Also check the email log if the plugin keeps one.\n<h3>3. Two Plugins Are Fighting Over the Same Hook</h3>\n<p>If you have <em>both</em> a form plugin and an SMTP plugin intercepting <code>wp_mail()</code>, and one was updated last Tuesday while the other wasn't, you can end up with a silent conflict. WordPress doesn't throw a visible error — it just resolves the conflict by dropping your emails.</p>\n<p><strong>How to check:</strong> Temporarily disable all plugins except your form plugin and one SMTP solution. Send a test. If it works, re-enable plugins one by one to find the culprit.</p>\n<h3>4. Your CRM Webhook Is Returning 200 but Dropping the Lead</h3>\n<p>Many agencies connect client forms directly to CRMs (HubSpot, Salesforce, ActiveCampaign) via webhooks or Zapier. The form plugin happily reports \"success\" because it got an HTTP 200 — but the CRM silently rejected the payload because:</p>\n<ul><li>A required field mapping changed</li>\n<li>The API key was rotated</li>\n<li>The field format doesn't match (e.g., phone number with parentheses when the CRM expects E.164)</li>\n<li>The integration user was deactivated</li>\n</ul>\n<strong>How to check:</strong> Go into your CRM's API logs or Zapier's task history. Look for the integration that handles form submissions and check recent runs. A 200 with an empty or error body is your red flag.\n<h3>5. Your Form Emails Are Landing in Spam (or Getting Blocked at the Server Level)</h3>\n<p>This is the sneakiest one because emails <em>are</em> sending — just not arriving. Common triggers:</p>\n<ul><li>The \"From\" address is set to the visitor's email (e.g., <code>from: visitor@gmail.com</code>), which SPF/DKIM checks flag as spoofing</li>\n<li>Your server IP landed on a blacklist</li>\n<li>The email content triggers spam filters (too many links, certain phrases)</li>\n<li>Your hosting provider silently rate-limits outbound email to 50/hour</li>\n</ul>\n<strong>How to check:</strong> If you have access to the receiving mail server, check the spam folder and mail logs. If not, temporarily switch the notification email to a Gmail address and test. If Gmail receives it but the client's business email doesn't, it's a spam-filtering issue on the client's side.\n<h2>Step-by-Step: Diagnose Your Broken Form in 10 Minutes</h2>\n<p>Run through these in order. Stop when you find the break.</p>\n<li><strong>Submit a test yourself.</strong> Use real-looking data. Wait 2 minutes. Nothing? Move on.</li>\n<li><strong>Check the form plugin's entry/lead storage.</strong> Plugins like Gravity Forms and WPForms store submissions in the database even when emails fail. If the entry is there but no email arrived, the form is working — email delivery is broken.</li>\n<li><strong>Send a plain WordPress test email.</strong> Use a mail-testing plugin or the SMTP plugin's test function. If that also fails, your server can't send mail at all.</li>\n<li><strong>Switch the \"From\" address to a domain you control.</strong> If your form uses the visitor's email as the From address, change it to <code>noreply@yourclientdomain.com</code>. SPF alignment matters.</li>\n<li><strong>Check plugin conflict.</strong> Disable everything except the form and one mail-delivery plugin. Test again.</li>\n<li><strong>Check CRM/webhook logs.</strong> If you're forwarding to a CRM, check whether the payload is arriving. A 200 doesn't always mean success.</li>\n<h2>The Fix, Depending on What You Found</h2>\n<ul><li><strong>PHP mail is dead:</strong> Install an SMTP plugin (WP Mail SMTP, Post SMTP, or FluentSMTP) and configure it with a transactional email service. SendGrid's free tier handles 100 emails/day — more than enough for form notifications.</li>\n</ul>\n<ul><li><strong>SMTP plugin broke:</strong> Regenerate the API key and update it. If using Microsoft 365, switch to an OAuth-based connector — basic auth is dead.</li>\n</ul>\n<ul><li><strong>Plugin conflict:</strong> Keep exactly one mail-delivery plugin. Delete the rest. They don't play nice together.</li>\n</ul>\n<ul><li><strong>CRM webhook silently failing:</strong> Check field mappings. Look for recently changed required fields. Re-authenticate the connection. Most CRMs let you re-send failed webhooks from the logs.</li>\n</ul>\n<ul><li><strong>Spam filtering:</strong> Use an SMTP service with proper SPF/DKIM setup. Set the From address to your domain. Avoid URL shorteners in notification emails. If the client's mail server is the problem, whitelist your sending domain.</li>\n</ul>\n<ul><li><strong>Nothing works and you're out of time:</strong> As a temporary bandage, set up email notifications through the form plugin's <em>built-in</em> storage (most have an \"entries\" view) or forward entries to a different email provider. But this is a manual stopgap, not a fix.</li>\n</ul>\n<h2>The Real Problem: You Found Out Late</h2>\n<p>Every fix above starts with the same sentence: \"I found out because someone told me.\"</p>\n<p>That's the actual problem. Forms break silently. Plugins update. API keys expire. Servers change configurations. And the breakage is invisible until a lead complains — or worse, never complains and just hires your client's competitor.</p>\n<p>The only reliable defense is checking before the lead does. Not once after a plugin update. Not when someone remembers. Every day, automatically.</p>\n<p>That's what we built LeadGuard for. It simulates a real visitor filling out your contact form every night, confirms the submission landed where it's supposed to, and alerts you if something broke — with a screenshot and a plain-English diagnosis so you're not decoding stack traces at 11 PM.</p>\n<p><strong>Want to see if your forms are working right now?</strong> <a href=\"https://leadguard.dev\" class=\"text-blue-600 hover:underline\">Run a free scan →</a> — paste your URL and LeadGuard will test your contact form, booking widget, and tracking pixels in under two minutes. No setup, no scripts, no credit card.</p>",
    "description": "You're scrolling through old emails and something catches your eye. A lead replied to a follow-up saying \"I filled out your contact form two weeks ago and ne..."
  },
  {
    "slug": "booking-widget-not-loading",
    "title": "Booking Widget Not Loading? Here's Why Your Client's Calendar Disappeared",
    "date": "2026-07-22",
    "excerpt": "You're on your third coffee, wrapping up the morning's client work, when your phone buzzes. It's the owner of that dental practice — your client of eighteen months. \"Hey, weird thing,\" she says. \"O...",
    "html": "<p>You're on your third coffee, wrapping up the morning's client work, when your phone buzzes. It's the owner of that dental practice — your client of eighteen months. \"Hey, weird thing,\" she says. \"Our front desk says nobody's booked online since last Tuesday. We thought it was slow, but then a patient mentioned the booking button just... didn't work.\"</p>\n<p>You pull up the site. The page looks fine. The headline is there, the services are there, the photos are there. But where the booking widget should be — that embedded calendar that was the centerpiece of the redesign you shipped six months ago — there's just white space. No error. No warning. Not even a broken image icon.</p>\n<p>The booking widget vanished, and the only person who noticed was a patient who cared enough to call.</p>\n<p>This happens to agencies constantly. Calendly, Acuity, Square Appointments — these embeds are the revenue backbone of service-based client sites. And they break in ways that leave zero trace.</p>\n<p>Here's exactly what goes wrong and how to fix it.</p>\n<h2>Why Booking Widgets Go Missing (The Silent Culprits)</h2>\n<h3>1. Ad Blockers and Tracker Blockers Eat the Script</h3>\n<p>This is the most common cause and the hardest one to catch, because it usually doesn't affect <em>you</em> — you whitelisted your client's site months ago. But your client's customers? Different story.</p>\n<p>Calendly and Acuity embeds load via third-party JavaScript. uBlock Origin, Privacy Badger, and even Firefox's Enhanced Tracking Protection flag these scripts as cross-origin trackers. The browser blocks them. The widget never renders. The visitor sees a blank space and assumes the site is broken.</p>\n<p>Even worse: Brave browser blocks Calendly's inline embed script by default. Safari's Intelligent Tracking Prevention can delay or block the script if the user hasn't interacted with the Calendly domain before.</p>\n<p><strong>How to tell:</strong> Open the site in an incognito window with no extensions — then open it in a fresh Brave install or Firefox with Strict tracking protection. If the widget loads in incognito but not in Brave/Firefox, it's a tracker-blocker issue.</p>\n<h3>2. The Consent Banner Blocks It Before It Loads</h3>\n<p>If the client's site has a cookie consent banner (Cookiebot, OneTrust, CookieYes), and the booking widget's domain isn't categorized as \"essential\" or \"functional,\" the consent manager blocks the third-party script until the user clicks \"Accept.\" Most visitors don't click anything — they just bounce.</p>\n<p>The widget's <code><iframe></code> or <code><script></code> tag never fires. The space where the calendar should be stays empty. No JavaScript error, because the script never ran in the first place.</p>\n<p><strong>This one is brutal to diagnose</strong> because it works fine for you (you already accepted cookies) and for anyone else who clicked \"Accept All.\" It only breaks for first-time visitors — which is exactly who needs to book.</p>\n<h3>3. The API Key or Embed Code Changed (And Nobody Told You)</h3>\n<p>Every booking platform uses a unique identifier in its embed code. For Calendly, it's the <code>url</code> parameter on the Calendly domain. For Acuity, it's the <code>owner</code> parameter in the scheduling URL. For Square Appointments, it's the <code>src</code> URL containing a site-specific token.</p>\n<p>These tokens break when:</p>\n<ul><li>The client changed their Calendly username or event type slug</li>\n<li>The client rotated their Acuity API key</li>\n<li>Someone at the client's company \"cleaned up\" their booking settings and deleted the event type you linked to</li>\n<li>The client switched from a free Calendly plan to paid (or vice versa) and the embed format changed</li>\n<li>Square updated the Appointments embed URL structure (they've done this twice)</li>\n</ul>\nThe embed code you pasted into the WordPress page or Squarespace code block still <em>looks</em> fine. It just points at a resource that no longer exists.\n<h3>4. The Iframe Doesn't Render on Mobile (But Works on Desktop)</h3>\n<p>Booking widgets are almost always embedded as iframes or script-injected containers. Both approaches have fixed dimensions — and on mobile viewports, those dimensions can push the widget into a two-pixel sliver or off-screen entirely.</p>\n<p>Common mobile-specific failures:</p>\n<ul><li>The iframe has <code>width=\"100%\"</code> but a <code>min-width=\"480px\"</code> inherited from the theme, so it overflows and the visible portion is blank</li>\n<li>A CSS media query hides the container <code>div</code> below a certain breakpoint</li>\n<li>A \"mobile optimization\" plugin lazy-loads iframes and miscalculates the viewport, never triggering the load</li>\n<li>The booking iframe needs a minimum height of 600px but the mobile layout gives it 200px — it loads, but it's invisible in the collapsed space</li>\n</ul>\nDesktop test passes. Mobile test fails. The client only hears about it when someone mentions they \"tried to book on their phone and couldn't.\"\n<h3>5. Plugin and Theme Conflicts</h3>\n<p>If the client's site runs on WordPress (most do), there are at least three things that can silently break a booking widget embed:</p>\n<ul><li><strong>Page builder updates:</strong> Elementor, Divi, or Beaver Builder updates can change how custom HTML/CSS blocks render. Your carefully pasted embed code might now be wrapped in an extra <code><div></code> that breaks the script's DOM targeting.</li>\n<li><strong>JavaScript optimization plugins:</strong> WP Rocket, Autoptimize, and others can defer, minify, or combine third-party scripts in ways that break the booking widget's initialization sequence. The script loads, but out of order, so the <code>Calendly.initInlineWidget()</code> call fires before the Calendly library is ready.</li>\n<li><strong>Security plugins:</strong> Wordfence or Sucuri can flag the booking widget's domain as suspicious and block the script request at the server level. The browser shows a CORS error in the console — but nobody's watching the console.</li>\n<li><strong>Lazy loading conflicts:</strong> If the theme or a performance plugin lazy-loads iframes, and the booking widget's script expects the iframe to exist immediately, the initialization fails silently.</li>\n</ul>\n<h3>6. The Client's Domain Got Added to Calendly's \"Restricted Sites\" List</h3>\n<p>This one is rare but devastating. Calendly allows users to restrict where their booking page can be embedded. If the client (or someone on their team) accidentally adds their own domain to the restricted list, the embed fails with a generic \"booking page not found\" message — or worse, a silent blank iframe.</p>\n<p>Acuity has a similar setting under \"Scheduling Page Linking.\" If the referring domain check fails, the embed shows nothing.</p>\n<h2>Diagnose It by Platform</h2>\n<h3>Calendly</h3>\n<li><strong>Check the embed code first.</strong> Open the page source (right-click → View Page Source, not Inspect Element). Search for <code>calendly</code>. If the inline embed code uses <code>Calendly.initInlineWidget({url: '...'})</code>, make sure the URL matches an active event type. Paste that URL directly into a browser — if <em>you</em> can't load it, neither can the embed.</li>\n<li><strong>Check the console.</strong> Open DevTools → Console. Look for:</li>\n   - <code>Calendly is not defined</code> — the Calendly library script didn't load (likely blocked)\n   - <code>Refused to frame 'https://calendly.com/'</code> — the domain is restricted or there's a framing policy issue\n   - <code>initInlineWidget</code> errors — the event type URL is invalid or the embed target element doesn't exist\n<li><strong>Test in an ad-blocked browser.</strong> Install Brave or enable uBlock Origin in Chrome. Load the page. If the widget doesn't appear, it's a tracker-blocking issue. Your fix options: proxy the booking page through your client's domain, or add a fallback button link.</li>\n<li><strong>Verify the Calendly script is actually loading.</strong> In DevTools → Network, filter by <code>calendly</code>. You should see <code>assets.calendly.com/assets/external/widget.js</code> (or similar). If the request is red (blocked) or missing entirely, the script is being stopped.</li>\n<h3>Acuity Scheduling</h3>\n<li><strong>Check the embed type.</strong> Acuity offers three embed methods: direct iframe, JavaScript embed, and the \"Scheduling Page Link.\" Iframes are the most reliable. JavaScript embeds break most often. Know which one you're using.</li>\n<li><strong>Test the <code>src</code> URL directly.</strong> Open the iframe <code>src</code> or the Acuity scheduling link in a new tab. If Acuity shows \"Scheduler not found\" or a login screen, the <code>owner</code> ID or scheduling page identifier has changed.</li>\n<li><strong>Check for mixed content.</strong> If the client's site loads over HTTPS but the Acuity embed uses an HTTP <code>src</code>, modern browsers block it as mixed content. Inspect the iframe <code>src</code> — if it starts with <code>http://</code>, change it to <code>https://</code>.</li>\n<li><strong>Look for CSS hiding the iframe.</strong> In DevTools, search for your Acuity iframe by its <code>src</code> attribute. If you find it in the DOM but it has <code>display: none</code> or <code>visibility: hidden</code> applied by a stylesheet, a CSS conflict is the culprit. Check the computed styles.</li>\n<h3>Square Appointments</h3>\n<li><strong>Verify the embed URL is current.</strong> Square changed its Appointments embed structure in 2024. Old embed codes using <code>squareup.com/appointments/book/...</code> might redirect or fail. The current embed URL pattern is <code>square.site/book/...</code> for Square Online sites. If you pasted the old format, replace it.</li>\n<li><strong>Check Square's site availability.</strong> Square Appointments embeds load from <code>square.site</code>. If the client's Square subscription lapsed, was downgraded, or the booking page was unpublished from the Square dashboard, the embed silently breaks. Log into the client's Square dashboard and confirm the booking page is published and active.</li>\n<li><strong>Test on mobile specifically.</strong> Square's embed can be especially finicky on mobile. In Chrome DevTools, toggle the device toolbar (Ctrl+Shift+M) and select iPhone 14 or Pixel 7. Reload the page. Scroll to where the widget should be. If it's not there, check if the container <code>div</code> is collapsing to zero height.</li>\n<li><strong>Look for redirect issues.</strong> Square sometimes inserts an interstitial or redirect before the booking page loads. If the client's site has a Content Security Policy (CSP) that doesn't allow redirects to <code>square.site</code>, the embed fails. Check the console for CSP violation messages.</li>\n<h2>The Fixes</h2>\n<p><strong>Ad blockers eating the script:</strong> You have two real options. Option one: add a visible \"Book Now\" button as a fallback link that opens the booking page in a new tab. It's not as pretty as the inline embed, but it works for every visitor, every time. Option two: proxy the booking page through the client's domain using a reverse proxy or a lightweight serverless function — now the booking widget loads from the same origin and ad blockers ignore it. Option two is more work but preserves the inline experience.</p>\n<p><strong>Consent banner blocking:</strong> In the consent manager's settings, categorize the booking widget's domain as \"essential\" or \"functional\" (not \"marketing\" or \"analytics\"). Essential scripts load before consent. If the CMP doesn't support granular categorization, add the booking widget's script domain to the allowed list. Test with a fresh browser that hasn't accepted cookies.</p>\n<p><strong>API key or embed code changed:</strong> Get the current embed code from the client's booking platform dashboard. Replace the old embed on the site. This takes five minutes. The real problem is you didn't know it changed — that's the monitoring problem we'll get to.</p>\n<p><strong>Mobile rendering failures:</strong> Inspect the widget container's CSS. Remove <code>min-width</code> constraints. Set <code>height</code> to <code>auto</code> or a viewport-relative unit like <code>min-height: 80vh</code>. If the theme has a mobile breakpoint that hides iframes, override it with a more specific CSS rule. Test across iPhone SE, iPhone 14, and a mid-range Android viewport — not just your own phone.</p>\n<p><strong>Plugin/theme conflicts:</strong> Disable JS optimization plugins one at a time and test the booking widget after each. If a specific plugin is the culprit, add the booking widget's script URL to that plugin's exclusion list (most optimization plugins have one). For security plugins, whitelist the booking platform's domain. For lazy-loading conflicts, add a <code>data-no-lazy=\"1\"</code> attribute to the iframe or script tag — most lazy-load plugins respect this.</p>\n<p><strong>Domain restrictions in the booking platform:</strong> Log into the booking platform dashboard. For Calendly, go to Scheduling Settings → \"Where can people book?\" and make sure the client's domain isn't in the block list. For Acuity, check Client's Scheduling Page → Advanced → Domain Restrictions.</p>\n<h2>How to Prevent This Entire Category of Problem</h2>\n<p>Look back at every cause above. Not one of them comes with a notification. Calendly doesn't email you when an ad blocker hides your widget. Acuity doesn't ping you when a consent banner blocks their script. WordPress doesn't tell you when a plugin update collides with your custom embed code.</p>\n<p>The only signal is the silence. And silence is expensive — it costs your client real bookings, for days or weeks, until someone speaks up.</p>\n<p>That's not a technology problem. It's a monitoring gap. And the fix isn't \"check every client site manually\" — that doesn't scale past five clients, and you've got twenty.</p>\n<p>The fix is automated verification. Every night, a real browser loads your client's booking page — not the homepage, the actual page where the widget lives — and confirms the booking interface is visible, clickable, and functional. If it isn't, you wake up to an alert instead of a panicked client call.</p>\n<h2>Want Your Booking Widgets Verified Every Night?</h2>\n<p>That's exactly what LeadGuard does. It crawls your client's site, finds every booking embed (Calendly, Acuity, Square, and more), and tests them nightly — as a real visitor, in a real browser. If a widget goes missing, you get a screenshot and a plain-English diagnosis before your client notices the silence.</p>\n<p>No scripts to install. No configuration. Paste a URL and LeadGuard handles the rest.</p>\n<p><strong><a href=\"https://leadguard.dev\" class=\"text-blue-600 hover:underline\">Run LeadGuard's free scan →</a></strong> — we'll test your client's booking widgets, contact forms, and tracking pixels in under two minutes. No credit card, no setup, no obligation.</p>",
    "description": "You're on your third coffee, wrapping up the morning's client work, when your phone buzzes. It's the owner of that dental practice — your client of eighteen ..."
  }
];

export default blogPosts;
